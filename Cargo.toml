[package]
name = "nvi-win"
version = "0.1.0"
edition = "2021"

[dependencies]
nvi = { path = "../nvi/crates/nvi" }

anyhow = "1.0.95"
rand = "0.8.5"
futures-util = "0.3.31"
thiserror = "2.0.9"
tokio = { version = "1.42.0", features = ["full"] }
tracing = "0.1.41"

[lints.rust]
missing_docs = "warn"

# Can't be used with any crate with examples or tests (!?). Best to enable this
# briefly, run cargo clippy (without --tests and --examples), fix unused deps,
# then disable it again.
# unused_crate_dependencies = "warn"

# unused_results = "warn"
# unreachable_pub = "warn"
# unqualified_local_imports = "warn"

[lints.clippy]
absolute_paths = "warn"
branches_sharing_code = "warn"
ignore_without_reason = "warn"
let_underscore_must_use = "warn"
many_single_char_names = "warn"
missing_docs_in_private_items = "warn"
multiple_inherent_impl = "warn"
needless_pass_by_value = "warn"
redundant_clone = "warn"
redundant_pub_crate = "warn"
self_named_module_files = "warn"
tests_outside_test_module = "warn"
use_self = "warn"
wildcard_dependencies = "warn"
cfg_not_test = "warn"
comparison_chain = "warn"
items_after_statements = "warn"
needless_pass_by_ref_mut = "warn"

# Settings with config values in clippy.toml
cognitive_complexity = "warn"
excessive_nesting = "warn"
too_many_lines = "warn"

[workspace.metadata.nanocode]
schema-version = 1
requires-nanocode = ">=0.1, <0.2"
features = "all"

[profile.dev]
debug = "line-tables-only"

[profile.dev.package."*"]
debug = false

# too_many_lines = "warn"
# multiple_crate_versions = "warn"
# module_name_repetitions = "warn"
# Not really usable - errors on names like min_x/min_y
# similar_names = "warn"
